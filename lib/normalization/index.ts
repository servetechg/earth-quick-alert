/** Phase 3 — unified hazard event shape and normalizers. */

export type { EventType, ConfidenceLevel, AlertLevel, UnifiedEvent } from './types';
export { normalizeAll, type RawSources } from './normalizer';
